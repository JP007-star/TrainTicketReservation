pipeline {
    agent any
     stages {
        stage('Initialize') {
            echo "Hello World"
            echo "${BUILD_NUMBER}"
        }
        stage('Run Test ') {
            echo "Hello World"
            echo "${BUILD_NUMBER}"
        }

     }

}